# ═══════════════════════════════════════════════════
# Variáveis de Ambiente — PRODUÇÃO
#
# ⚠️  NUNCA commite este arquivo preenchido no Git.
#     Copie para .env e preencha com valores reais.
#     Use um gerenciador de segredos (Vault, AWS SSM,
#     GCP Secret Manager) sempre que possível.
#
# Gerar SECRET_KEY segura:
#   python -c "import secrets; print(secrets.token_hex(32))"
# ═══════════════════════════════════════════════════

# [OBRIGATÓRIO] Chave secreta do Flask — app NÃO sobe sem ela
SECRET_KEY=

# [OPCIONAL] Senha do banco de dados
DB_PASSWORD=

# [OPCIONAL] Caminho do banco SQLite
DATABASE_PATH=/var/data/users.db

# [OPCIONAL] Configuração do Gunicorn
GUNICORN_WORKERS=4
GUNICORN_THREADS=4
PORT=5000

# [OPCIONAL] Rate limiting
RATE_LIMIT_MAX=30
RATE_LIMIT_WINDOW=60

# [OPCIONAL] Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# [OPCIONAL] IPs confiáveis para X-Forwarded-For (Nginx/ALB)
FORWARDED_ALLOW_IPS=127.0.0.1
